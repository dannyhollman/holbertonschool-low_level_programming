#include <stdio.h>

int main(void)
{
	int i;

	for (i = 0; i < 100; i++)
	{
		putchar((i / 10));
		putchar((i % 10));
		if (i != 99)
		{
			putchar(',');
			putchar(' ');
		}
	}
	putchar('\n');
	return (0);
}
