#include "holberton.h"
#include <stdio.h>
/**
  * main - prints program name
  * @argc: number of arguments
  * @argv: array of arguments
  * Return: argc to avoid unused argument error
  */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (argc);
}
